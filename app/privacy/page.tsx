import { StaticPage } from "@/components/site";

export default function Page() {
  return <StaticPage title="Privacy" body="No user accounts and no built-in tracking. The site serves content from the repo." path="/privacy" />;
}
